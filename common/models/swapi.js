'use strict';

module.exports = function (Swapi) {
 

    // Swapi.afterRemote('getships', function (ctx, news, next) {
    //     var ships = ctx.result;
    //     ships.map(function (ships) {

    //         //Remove unwanted properties returned from the API here
    //         // delete news.created;
    //         // delete news.id;
    //         return ships;
    //     });
    //     //the new news
    //     ctx.result = ships;

    //     cache[i] = { "time": Date.now(), "news": ships, "param": ctx.args.name };
    //     console.log("Wrote to cache", cache[i].param);
    //     i++;

 
    //     next();

    // });

};
